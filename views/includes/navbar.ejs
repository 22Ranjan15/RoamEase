<!-- Main Navigation Bar -->
<nav class="navbar navbar-expand-lg bg-white border-bottom sticky-top shadow-sm">
    <div class="container-fluid px-3">
        <!-- Brand Logo and Name (Airbnb style: logo left, search center, user right) -->
        <div class="d-flex w-100 align-items-center justify-content-between">
            <!-- Left: Logo -->
            <a class="navbar-brand fw-bold d-flex align-items-center gap-2 brand-logo" href="/listings">
                <i class="fa-regular fa-compass"></i>
                <span class="brand-text">RoamEase</span>
            </a>
            <!-- Center: Search Bar -->
            <form class="d-none d-lg-flex mx-auto" role="search" action="/listings/search" method="GET" style="max-width: 400px; width:100%;">
                <div class="search-container position-relative w-100">
                    <input class="form-control search-input rounded-pill px-4 py-2" 
                           type="search" 
                           name="query"
                           placeholder="Search destinations..." 
                           aria-label="Search"
                           id="searchInput"
                           autocomplete="off"
                           style="box-shadow:0 2px 8px rgba(0,0,0,0.07);">
                    <button class="btn search-btn d-flex align-items-center justify-content-center" type="submit" style="background:#fe424d; border-radius:50%; width:40px; height:40px; position:absolute; top:50%; transform:translateY(-50%);">
                        <i class="fa-solid fa-magnifying-glass" style="color:#fff;"></i>
                    </button>
                </div>
            </form>
            <!-- Right: Host & Auth -->
            <ul class="navbar-nav flex-row align-items-center gap-2 mb-0">
                <li class="nav-item d-none d-lg-block">
                    <a class="nav-link fw-semibold text-dark hover-primary" href="/listings/new">Become a host</a>
                </li>
                <% if(!currUser) { %>
                    <li class="nav-item d-none d-lg-block">
                        <a class="nav-link fw-bold text-primary" href="/signup">Sign up</a>
                    </li>
                    <li class="nav-item d-none d-lg-block">
                        <a class="nav-link fw-bold text-primary" href="/login">Login</a>
                    </li>
                <% } %>
                <% if(currUser) { %>
                    <li class="nav-item d-none d-lg-block">
                        <a class="nav-link fw-semibold text-dark hover-primary d-flex align-items-center" href="/profile">
                            <div class="user-avatar bg-primary text-white rounded-circle d-flex align-items-center justify-content-center me-2">
                                <%= currUser.username.charAt(0).toUpperCase() %>
                            </div>
                            <span class="d-none d-lg-inline"><%= currUser.username %></span>
                        </a>
                    </li>
                    <li class="nav-item d-none d-lg-block">
                        <a class="nav-link fw-semibold text-danger" href="/logout">
                            <i class="fa-solid fa-sign-out-alt me-2"></i>Logout
                        </a>
                    </li>
                <% } %>
            </ul>
            <!-- Mobile: Hamburger -->
            <button class="navbar-toggler border-0 d-lg-none ms-2" type="button" data-bs-toggle="collapse" data-bs-target="#navbarScroll" aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
        <!-- Collapsible Navbar Content (Mobile) -->
        <div class="collapse bg-light py-3 d-lg-none" id="navbarScroll" style="position:fixed; top:64px; left:0; width:100vw; z-index:1050; border-radius:0 0 1rem 1rem; box-shadow:0 2px 16px rgba(0,0,0,0.08); overflow-x:auto;">
            <ul class="navbar-nav flex-column gap-2 align-items-center w-100">
                <li class="nav-item">
                    <a class="nav-link fw-semibold text-dark hover-primary" href="/listings">Explore</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link fw-semibold text-dark hover-primary" href="/listings/new">Become a host</a>
                </li>
                <li class="nav-item w-100">
                    <form class="d-flex justify-content-center w-100" role="search" action="/listings/search" method="GET" style="max-width: 400px; margin: 0 auto;">
                        <div class="search-container position-relative w-100">
                            <input class="form-control search-input rounded-pill px-4 py-2" 
                                   type="search" 
                                   name="query"
                                   placeholder="Search destinations..." 
                                   aria-label="Search"
                                   id="searchInputMobile"
                                   autocomplete="off"
                                   style="box-shadow:0 2px 8px rgba(0,0,0,0.07);">
                            <button class="btn search-btn d-flex align-items-center justify-content-center" type="submit" style="background:#fe424d; border-radius:50%; width:40px; height:40px; position:absolute; top:50%; transform:translateY(-50%);">
                                <i class="fa-solid fa-magnifying-glass" style="color:#fff;"></i>
                            </button>
                        </div>
                    </form>
                </li>
                <% if(!currUser) { %>
                    <li class="nav-item">
                        <a class="nav-link fw-bold text-primary" href="/signup">Sign up</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link fw-bold text-primary" href="/login">Login</a>
                    </li>
                <% } %>
                <% if(currUser) { %>
                    <li class="nav-item">
                        <a class="nav-link fw-semibold text-dark hover-primary d-flex align-items-center" href="/profile">
                            <div class="user-avatar bg-primary text-white rounded-circle d-flex align-items-center justify-content-center me-2">
                                <%= currUser.username.charAt(0).toUpperCase() %>
                            </div>
                            <span><%= currUser.username %></span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link fw-semibold text-danger" href="/logout">
                            <i class="fa-solid fa-sign-out-alt me-2"></i>Logout
                        </a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>


